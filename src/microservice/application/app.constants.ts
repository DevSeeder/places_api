export const PROJECT_KEY = 'PET_HELPER';

export enum DependencyEntityTokens {
  COUNTRIES = 'countries',
  STATES = 'states',
  CITIES = 'cities',
  USER = 'users'
}

export enum DIToken {
  SCOPE_KEY = 'SCOPE_KEY',
  PROJECT_KEY = 'PROJECT_KEY'
}
